import { Text } from "react-native";

import { DefaultLayout } from "../../layouts/DefaultLayout";

export const HomeScreen = () => {
	return (
		<DefaultLayout>
			<Text>Home</Text>
		</DefaultLayout>
	);
};
